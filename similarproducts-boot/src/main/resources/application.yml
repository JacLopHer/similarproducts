server:
  port: 5000

spring:
  application:
    name: similarproducts
  profiles:
    active: dev

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    path: /v3/api-docs

# Configuración de APIs externas con resiliencia
external-api:
  base-url: http://localhost:3001
  endpoints:
    similar-ids: /product/{productId}/similarids
    product-detail: /product/{productId}
  timeout:
    # Timeout de conexión: 5 segundos
    connect-ms: 5000
    # Timeout de lectura: 10 segundos
    read-ms: 10000
  # Pool de conexiones (configurado en HttpClientConfig)
  connection-pool:
    max-connections: 50
    pending-acquire-max: 100
    acquire-timeout-ms: 5000
    max-idle-time-ms: 30000
  # Reintentos
  retry:
    max-attempts: 2
    retry-delay-ms: 100

---
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    root: INFO
    com.example.similarproducts: DEBUG
    org.springframework.web.reactive.function.client: DEBUG
    reactor.netty.http.client: DEBUG
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

---
spring:
  config:
    activate:
      on-profile: test

logging:
  level:
    root: WARN
    com.example.similarproducts: INFO
    org.springframework.web.reactive.function.client: WARN
    reactor.netty.http.client: WARN

---
spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: INFO
    com.example.similarproducts: INFO
    org.springframework.web.reactive.function.client: WARN
    reactor.netty.http.client: WARN
  pattern:
    console: "%d{ISO8601} [%thread] %-5level %logger{36} - %msg%n"

